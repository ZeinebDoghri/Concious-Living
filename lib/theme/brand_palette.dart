import 'package:flutter/material.dart';

// ── LEGACY (kept for backward compat) ──────────────────────────────────────────
const Color kOat = Color(0xFFEDE0D3);
const Color kParchment = Color(0xFFFAF5EE);
const Color kSand = Color(0xFFD9C9B4);
const Color kCocoa = Color(0xFF5C3D3F);
const Color kEspresso = Color(0xFF2C1A1B);
const Color kFog = Color(0xFF8C7B7C);

const Color kCherry = Color(0xFF75070C);
const Color kCherryMid = Color(0xFF9E1A21);
const Color kCherryB = Color(0xFFFBBCBF);

const Color kButter = Color(0xFFFFEDAB);
const Color kButterD = Color(0xFFE8C84A);
const Color kButterDeep = Color(0xFFB8860B);

const Color kOlive = Color(0xFF4F6815);
const Color kOliveMid = Color(0xFF3A5010);
const Color kOliveM = Color(0xFFD4E8A8);
const Color kOliveLight = Color(0xFFEEF5DC);

const Color kInfo = Color(0xFF185FA5);
const Color kInfoBg = Color(0xFFE6F1FB);

const Color kViewfinderHotel = Color(0xFF1A0A0B);
const Color kViewfinderCustomer = Color(0xFF1A1500);
const Color kViewfinderRestaurant = Color(0xFF0D1A05);

// ── v4 BASE — Light & Warm ─────────────────────────────────────────────────────
const Color kBg      = Color(0xFFF7F3EE);   // warm light background
const Color kCard    = Color(0xFFFFFFFF);
const Color kBorder  = Color(0xFFE8DDD2);
const Color kText2   = Color(0xFF7A6E66);   // body text — warm medium
const Color kText3   = Color(0xFFB0A49A);   // captions — warm light
const Color kSurface = Color(0xFFFDF9F6);   // elevated surface
const Color kSuccess = Color(0xFF44B070);
const Color kWarning = Color(0xFFF0A030);
const Color kDanger  = Color(0xFFE05555);

// ── v4 CUSTOMER · Soft Lilac ──────────────────────────────────────────────────
const Color kCust1   = Color(0xFFC8A8E8);   // primary lilac
const Color kCust2   = Color(0xFF9B72CC);   // deep lilac
const Color kCust3   = Color(0xFFF8F4FF);   // surface lilac
const Color kCust4   = Color(0xFFE2D0F8);   // soft lilac

// ── v4 RESTAURANT · Rose Coral ────────────────────────────────────────────────
const Color kRest1   = Color(0xFFF2A7A7);   // primary coral rose
const Color kRest2   = Color(0xFFD4706E);   // deep coral
const Color kRest3   = Color(0xFFFFF5F5);   // surface
const Color kRest4   = Color(0xFFFFD6D6);   // soft rose

// ── v4 HOTEL · Sage Green ─────────────────────────────────────────────────────
const Color kHotel1  = Color(0xFF7DC5A0);   // primary sage
const Color kHotel2  = Color(0xFF3E8A65);   // deep sage
const Color kHotel3  = Color(0xFFF4FAF7);   // surface
const Color kHotel4  = Color(0xFFC8EDD9);   // soft sage
